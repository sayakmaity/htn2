var first;
var last;
var healthCard;
var postal;

function nextPage() {
    // first = document.getElementById('first').value;
    // last = document.getElementById('last').value;
    // healthCard = document.getElementById('healthCard').value;
    // postal = document.getElementById('postal').value;

    // open incident.html
    window.location.href = "incident.html";


    // client.connect(function (err) {
    //     if (err) throw err;
    //     console.log("Connected!");
    //     var sql = "INSERT INTO users.one (id, firstName, lastName, zip) VALUES ('" + healthCard + "', '" + first + "', '" + last + "', '" + postal + "')";
    //     client.query(sql, function (err, result) {
    //         if (err) throw err;
    //         console.log("1 record inserted");
    //     });
    // });
}


function nextPage2() {


    // open ubereats.html
    window.location.href = "ubereats.html";

    

    // client.connect(function (err) {
    //     if (err) throw err;
    //     console.log("Connected!");
    //     var sql = "INSERT INTO users.one (id, firstName, lastName, zip) VALUES ('" + healthCard + "', '" + first + "', '" + last + "', '" + postal + "')";
    //     client.query(sql, function (err, result) {
    //         if (err) throw err;
    //         console.log("1 record inserted");
    //     });
    // });
}

